{
  "private": true,
  "scripts": {
	"dev": "turbo run dev",
	"watch": "turbo run watch",
	"build": "turbo run build",
	"build:client": "turbo run build --scope=@prioritea/client",
	"build:server": "turbo run build --scope=@prioritea/server"
  },
  "devDependencies": {
	"@types/jest": "^27.5.0",
	"eslint": "^8.14.0",
	"husky": "^7.0.4",
	"plop": "^3.1.0",
	"prettier": "^2.6.2",
	"rimraf": "^3.0.2",
	"turbo": "^1.2.5"
  }
}
