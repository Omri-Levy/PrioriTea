upstream server {
	server server:4000;
}

server {
  listen 80;

  location / {
    root /var/www/html;
    index index.html index.htm;
    try_files $uri  $uri/ /index.html;
  }

  location /api/ {
	rewrite /api/(.*) /$1 break;
	proxy_pass http://server;
 }

}
